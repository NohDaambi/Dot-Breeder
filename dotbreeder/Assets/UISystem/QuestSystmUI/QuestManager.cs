using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class QuestManager : MonoBehaviour
{
     
  
    void Start()
    {
        //Clear하지 않은 퀘스트파일만 불러온다.
        //현재 진행가능한 지역의 퀘스트만 불러온다.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
